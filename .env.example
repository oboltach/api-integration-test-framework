# Environment
ENV=dev

# API
BASE_URL=https://api.example.com

# Test users (replace locally)
ADMIN_USERNAME=admin_user
ADMIN_PASSWORD=changeme
SERVICE_TOKEN=changeme

# Database (local / test only)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=users_service
DB_USER=test_user
DB_PASSWORD=changeme
DB_SSL=false

# Safety flags
ALLOW_DB_TESTS=false
